[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.build]
cmds = [
  "cd rubolka-nextjs",
  "npm ci --only=production",
  "npm run build"
]

[start]
cmd = "cd rubolka-nextjs && npm start" 